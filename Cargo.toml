[package]
name = "mitos-init"
version = "0.1.0"
edition = "2021"
authors = ["Your Name <babor3331@gmail.com>"]
description = "The PID 1 initialization system for MITOS"

[dependencies]
# We will use the 'nix' crate later for system calls like mount() and waitpid()
nix = { version = "0.27", features = ["process", "mount", "signal"] }
